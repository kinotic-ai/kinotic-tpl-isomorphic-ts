{
	"extends": "../../tsconfig.base.json",
	"compilerOptions": { "declaration": true, "isolatedDeclarations": true },
	// "files" is intentionally empty so `type-check` passes on a freshly created
	// project, before any entities have been written.
	"files": [],
	"include": ["model/**/*", "repositories/**/*"]
}
